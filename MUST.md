**Notes from the project:**

* Overall, the assistant fully agreed with the project; she only mentioned these two things:

  * The number of models should be **2**, there is no need for more than that.
  * The datasets should be **2 datasets**, but we should apply **both types of pressure to both datasets**, meaning that in total we will have **4 datasets**:

    * **Domain** with the first type of pressure
    * **Domain** with the second type of pressure
    * **Out of the domain** with the first type of pressure
    * **Out of the domain** with the second type of pressure.


NOTE: The rule percentage of 70/30 should be modified inside the other markdown files, and be implemented based on these notes.
Use cais/mmlu dataset.
